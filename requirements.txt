# Crawl
scrapy>=2.11
playwright>=1.45

# Extraction
extruct>=0.18
trafilatura>=1.9

# Matching / validation
rapidfuzz>=3.9
pydantic>=2.7

# API + dashboard
fastapi>=0.111
uvicorn>=0.30

# Data handling
pandas>=2.2
orjson>=3.10

# Testing
pytest>=8.2
pytest-asyncio>=0.23

# Registry client (evaluate before depending on — see docs/RESEARCH.md)
# brreg>=1.0.0a1

# Matching upgrade path, if RapidFuzz threshold matching proves
# insufficient on the dev slice (see docs/RESEARCH.md §5)
# splink>=3.9

# Dev tooling
ruff>=0.5
python-dotenv>=1.0
